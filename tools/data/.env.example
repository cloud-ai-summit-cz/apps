# Environment configuration for toy data scripts

# Toy service API endpoint
TOY_SERVICE_URL=http://localhost:8001

# Path to authentication token (relative or absolute)
AUTH_TOKEN_PATH=../identity/auth_token.json

# Data file paths (optional - defaults shown)
TOY_PROFILES_JSON=toy_profiles.json
TOY_IMAGES_FOLDER=toy-images
