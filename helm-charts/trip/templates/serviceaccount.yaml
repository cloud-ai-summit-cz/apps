{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "trip.serviceAccountName" . }}
  labels:
    {{- include "trip.labels" . | nindent 4 }}
  {{- $clientId := tpl (default "" .Values.workloadIdentity.clientId) . }}
  {{- $wiEnabled := and .Values.workloadIdentity.enabled (ne $clientId "") }}
  {{- if or $wiEnabled .Values.serviceAccount.annotations }}
  annotations:
    {{- if $wiEnabled }}
    azure.workload.identity/client-id: {{ $clientId | quote }}
    azure.workload.identity/tenant-id: {{ .Values.auth.tenantId | quote }}
    {{- end }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
{{- end }}
