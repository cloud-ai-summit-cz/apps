# Default values for platform-cert-manager

# Environment: staging or production
# This determines which Let's Encrypt server to use
environment: staging

# Let's Encrypt configuration
letsencrypt:
  email: "your-email@example.com"  # TODO: Update with actual email
  
  # ACME server URLs
  staging: "https://acme-staging-v02.api.letsencrypt.org/directory"
  production: "https://acme-v02.api.letsencrypt.org/directory"
  
  # HTTP-01 challenge uses AKS App Routing ingress class
  ingressClassName: "webapprouting.kubernetes.azure.com"

# cert-manager Helm chart values
cert-manager:
  crds:
    enabled: true
  installCRDs: true
