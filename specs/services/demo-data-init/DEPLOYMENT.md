## Demo Data Init Deployment

- Deployed as a FastAPI service behind the shared Gateway
- Single `/demo-data/import` endpoint for triggering imports
- Enabled only in non-production environments by default
