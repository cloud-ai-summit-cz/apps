# Toy Service - Staging Environment Values
# This file is automatically updated by CI/CD pipeline with image tags

image:
  repository: "crappdemoeniwvl.azurecr.io/toy" # Set by CI: <acr-login-server>/toy-service
  tag: "b51db6e44d73bb602565062b1c1c14ed38dc502b" # Set by CI: commit SHA
replicaCount: 2
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi
httpRoute:
  enabled: true
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: web-frontend-gateway
      namespace: toytrip-staging
      sectionName: https
  hosts:
    - host: "appdemo-eniwvl.swedencentral.cloudapp.azure.com"
      paths:
        - path: /api/toys
          pathType: PathPrefix
env:
  # Auth and infrastructure values come from azure-values.yaml
  COSMOS_CONTAINER_NAME: "toys"
  BLOB_CONTAINER_AVATARS: "avatars"
  API_HOST: "0.0.0.0"
  API_PORT: "8001"
  LOG_LEVEL: "INFO"
workloadIdentity:
  enabled: false
  clientId: "" # Set when workload identity is configured
